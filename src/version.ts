// Kept in sync with package.json "version". Sent as the `$lib_version` property
// and the replay `lib` tag so server-side debugging can pin client versions.
export const VERSION = "0.1.0";
export const LIB_NAME = "pulse-js";
